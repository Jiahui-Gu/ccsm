{
  "name": "@ccsm/daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CCSM daemon (headless RPC server). Skeleton only; implementation lands in subsequent T0.x / T1.x tasks per design spec ch11.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\" \"eslint-plugins/**/*.{js,ts}\" \"build/**/*.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run",
    "pretest": "node ../../scripts/ensure-native-abi.mjs node",
    "build:sea:posix": "bash build/build-sea.sh",
    "build:sea:win": "pwsh -ExecutionPolicy Bypass -File build/build-sea.ps1",
    "stage:native:posix": "bash build/stage-native.sh dist/native",
    "stage:native:win": "pwsh -ExecutionPolicy Bypass -File build/stage-native.ps1 -OutDir dist/native",
    "sign:mac": "bash build/sign-mac.sh",
    "sign:linux": "bash build/sign-linux.sh",
    "sign:win": "pwsh -ExecutionPolicy Bypass -File build/sign-win.ps1",
    "build:msi": "pwsh -ExecutionPolicy Bypass -File build/install/win/build-msi.ps1",
    "build:pkg:mac": "bash build/install/mac/build-pkg.sh",
    "build:pkg:linux": "bash build/install/linux/build-pkg.sh",
    "prebuildify:better-sqlite3": "prebuildify --napi --strip --target node@22.0.0 --cwd node_modules/better-sqlite3",
    "prebuildify:node-pty": "prebuildify --napi --strip --target node@22.0.0 --cwd node_modules/node-pty"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.12.0",
    "@connectrpc/connect": "^2.1.1",
    "@xterm/addon-serialize": "^0.13.0",
    "@xterm/headless": "^5.5.0",
    "better-sqlite3": "^12.9.0",
    "node-pty": "1.1.0"
  },
  "devDependencies": {
    "@bufbuild/protobuf": "^2.12.0",
    "@ccsm/proto": "workspace:*",
    "@connectrpc/connect": "^2.1.1",
    "@connectrpc/connect-node": "^2.1.1",
    "@types/better-sqlite3": "^7.6.12",
    "ajv": "^8.20.0",
    "prebuildify": "^6.0.1"
  }
}
