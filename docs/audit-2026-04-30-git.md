# Audit Report — Git/PR Hygiene (D6) — 2026-04-30

## Summary

| Severity | Item | Count |
|---|---|---|
| HIGH | Residual stashes in shared object DB | 32 |
| HIGH | Worktree branches orphaned from origin | 2 (pool-3, pool-4) |
| MED  | Unmerged remote branches >7d old | 86 |
| MED  | Open PR stale (1 open, draft, throwaway-tagged) | 1 (#427) |
| LOW  | Detached worktree HEAD | 1 (pool-1) |
| LOW  | Stale remote branches >30d unmerged | 0 |
| LOW  | Recent commit-message hygiene (last 30 on `working`) | 0 violations |

## 1. Stale remote branches (>30d, never merged) — LOW

None. Oldest remote branch is `origin/feat/migrate-ndjson` at 2026-04-21 (9 days). Repo is young / aggressively pruned.

## 2. Unmerged remote branches >7d — MED (86 branches)

86 branches haven't merged to `origin/working` and are >7d old. The 2026-04-21 cohort (20 branches: `feat/migrate-*`, `feat/file-tree`, `feat/terminal-pane`, `fix/ask-user-question-render`, `fix/visual-token-alignment`, `feat/session-restore`, `feat/endpoint-model-discovery`, etc.) and 2026-04-22 cohort (`feat/system-notifications`, `feat/ui-shell-redesign`, `feat/slash-commands`, `feat/drag-drop-images`, `feat/release-infra`, etc.) look like an abandoned migration spike series — many overlap with what later merged via different branch names.

Manager triage candidates. Per branch:
```bash
git log --oneline origin/working..origin/feat/migrate-ndjson | head
gh pr list --state all --search "head:feat/migrate-ndjson"
# If superseded:
git push origin --delete feat/migrate-ndjson
```

## 3. Open PRs >7d no activity — MED (1 PR, 3d old, draft)

| # | Title | Branch | Author | Created | Updated | Comments | Commits |
|---|---|---|---|---|---|---|---|
| 427 | spike: ttyd + webview embed claude CLI (throwaway) | `spike/ttyd-embed` | Jiahui-Gu | 2026-04-27 | 2026-04-27 | 0 | 1 |

Draft, no comments, 3d quiet, title self-identifies as throwaway.
```bash
gh pr close 427 --comment "Closing throwaway spike per D6 audit."
git push origin --delete spike/ttyd-embed
```

## 4. Residual stashes — HIGH (32 stashes, shared across all 10 pools)

All 10 pools report identical 32 — shared object database. Notable:
- `stash@{2}` On `fix-unify-app-icon-taskbar-tray`: `pool-2-pre-review-stash`
- `stash@{5}` On `fix-close-dialog-native`: `pool-2-pre-icon-fix`
- `stash@{7}` On `fix-import-session-empty-cli`: `pool-4 leftover before PR517 rebase`
- `stash@{8}` On `fix-agenticon-halo-not-on-active`: `abandoned-AgentIcon-prop-attempt`
- `stash@{22}` On `worker/554-zombie-prevent`: `zombie-fix-temp`
- `stash@{26}` On `working`: `main repo dogfood docs+screenshots WIP`
- `stash@{29}` On `fix-context-chip-always-visible-v2`: `fp13c-fix-temporary`
- 12+ "WIP on (no branch)" entries — branches are gone.

Many tagged `WIP`/`temp`/`abandoned` — strong drop candidates.
```bash
git stash list
git stash show -p stash@{N} | head -50
git stash drop stash@{N}   # NOTE indices shift; drop highest N first
```

## 5. Worktree branches orphaned from origin — HIGH

| Pool | Branch | Status |
|---|---|---|
| pool-1 | (detached HEAD) | LOW — finish or check out a branch |
| pool-2 | `refactor/818-db-save-event-bus` | OK |
| **pool-3** | **`reviewer-cleanup`** | **ORPHAN — no origin counterpart** |
| **pool-4** | **`refactor/main-extract-ipc-lifecycle`** | **ORPHAN — already merged & deleted (PR #578)** |
| pool-5 | `chore/800-electron-major-sqlite-cobump` | OK |
| pool-6 | `test/765-rtl-gap-fill` | OK |
| pool-7 | `test/766-sidebar-rtl-backfill` | OK |
| pool-8 | `refactor/sidebar-trim-phase-b` | OK |
| pool-9 | `refactor/e2e-batch-3-1-delete-probes` | OK |
| pool-10 | `refactor/e2e-batch-3-2-trim` | OK |

Cleanup:
```bash
# pool-3: if reviewer-cleanup truly dead:
cd ~/ccsm-worktrees && git worktree remove pool-3 && git branch -D reviewer-cleanup

# pool-4 (branch already merged via #578):
cd ~/ccsm-worktrees/pool-4 && git fetch origin && git checkout working && git reset --hard origin/working

# pool-1 (detached): attach to a branch.
```

## 6. Recent commit-message hygiene (last 30 on `origin/working`) — LOW

Zero violations. All 30 most-recent commits follow `<type>(<scope>): <msg>` Conventional Commits format. No `WIP`, `oops`, `fixup`, `typo`, `temp`, `debug` markers.

## Prioritized cleanup queue

1. **HIGH** — Resolve pool-3, pool-4 orphan worktree branches. Re-attach pool-1.
2. **HIGH** — Triage 32 stashes; `WIP`/`temp`/`abandoned`/`(no branch)` entries are obvious drops.
3. **MED** — Close PR #427 + delete branch `spike/ttyd-embed`.
4. **MED** — Sweep 2026-04-21/22 unmerged remote branch cohorts (~50 of 86 stale).
5. **LOW** — Continue current commit-message discipline.
