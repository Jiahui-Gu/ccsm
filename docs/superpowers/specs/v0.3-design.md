# v0.3 design — daemon split

## v0.3 SHIP GOAL — 无中间态, 零 v0.4 返工 (2026-05-02 frozen)

**口径**: v0.3 落地的每一行代码, 在 v0.4 加 web/iOS/远程时**一行不删一行不改**。v0.3 = `2026-05-02-final-architecture.md` 的**真子集**, 不是过渡形态。任何 "v0.4 再切 / v0.4 再扩 / v0.4 替换" 的做法在 v0.3 PR review 阶段直接 REQUEST-CHANGES。

### v0.3 必须做 (做完 v0.4 不返工)

1. **`proto/` schema 一次到位** — 含 v0.4 全部 service (`pty` / `sessions` / `db` / `crash` / `daemon`) + server-streaming 签名。`buf breaking` 上 CI。v0.4 不改 schema, 只多生成 web/swift 客户端。
2. **Connect-RPC over HTTP/2 数据面** — `daemon/src/connect/` Connect-Node server, 所有数据面 RPC 走它。零 envelope 数据面代码。
3. **Listener A** (peer-cred UDS / 命名管道) — desktop 直连。peer-cred 信任绑 transport, 不绑 header。
4. **Listener B** (`127.0.0.1:PORT_TUNNEL`) **物理 bind + JWT interceptor 完整实现** — 即使 v0.3 没人连它, listener 必须存在, JWT 校验代码 + UT 必须写完。v0.4 接 cloudflared = 打开开关, 不是加代码。
5. **Supervisor 控制面保留 envelope** — `/healthz` / `daemon.hello` / `shutdown*` 不动。但 `daemon.hello` 的 hello-HMAC 摘掉 (auth 改走 peer-cred + JWT)。
6. **Daemon 不是 Electron 子进程** — daemon OS-lifecycle 从 day 1 立住, 即使 v0.3 只有 desktop 用, daemon 也必须能脱离 Electron 活。
7. **Session 模型 backend-authoritative + snapshot+delta + broadcast+LWW** — PTY host 一上来就按 N≥3 客户端 fan-out 写, 不是 "先 N=1 后扩"。RAM-only scrollback。
8. **Electron = 纯 thin client** — `@connectrpc/connect-node` 打 Listener A, 零业务逻辑, 零本地状态 (除渲染缓存)。

### v0.3 不做 (但留接口/留位置, 不是留 TODO)

| 不做 | 留什么 |
|---|---|
| cloudflared sidecar 进程管理 | Listener B 已 bind, 等 sidecar 来连 |
| Web 客户端 `web/` | proto schema 已含全部 service |
| iOS 客户端 | 同上 |
| OS supervisor (headless mode) | daemon 已 detach Electron, 加 launchd/service 是外挂 |
| Scrollback 持久化 | session 模型已 authoritative, 加持久化是 PTY host 内部加层 |
| Multi-machine | — |

### 反模式 (PR review REQUEST-CHANGES 触发器)

- ❌ "先用 envelope 做数据面, v0.4 再切 Connect" — 直接上 Connect。
- ❌ "Listener B 先不 bind, v0.4 再加" — 必须 bind, JWT interceptor 必须完整 + UT。
- ❌ "PTY host 先 N=1, v0.4 扩 fan-out" — 一上来就 broadcast+LWW。
- ❌ "Electron 主进程暂留点业务逻辑" — 全下沉 daemon。
- ❌ "hello-HMAC 先留着兼容旧客户端" — 直接删, 旧客户端不存在。
- ❌ 任何 `// TODO v0.4` / `// will be replaced` / `// temporary` 注释。

### 全局架构权威

`docs/superpowers/specs/2026-05-02-final-architecture.md` 是 v0.3 + v0.4 全局架构唯一权威。本文件是 v0.3 落地切片, 与 final architecture 冲突时以 final architecture 为准。

---

> **Status**: design spec, r12 final (0 P0 across 7 review angles, 7 review rounds completed; perMachine reconciliation locked at r12, 2026-05-01).
> **Scope**: refactor only — no added or removed features versus v0.2 (verified by feature-parity reviewer in r6 + r8). New behaviors (`daemon.shutdownForUpgrade` RPC, debugger inspect ports, install marker file) are all justified as P0 necessities of the daemon split per the feature-parity angle.
> **Source of truth**: the seven fragment files under `v0.3-fragments/` are the canonical spec. This file is the index/TOC. Companion exec summary: [`v0.3-daemon-split.md`](./v0.3-daemon-split.md). Plan: [`docs/superpowers/plans/2026-04-30-v0.3-daemon-split.md`](../plans/2026-04-30-v0.3-daemon-split.md). Plan-delta totals tracked in follow-up #941.

The v0.3 daemon split moves the long-running side of CCSM (PTY supervision, SDK calls, persistence) into a standalone daemon process, separate from the Electron app. The Electron client connects over a local IPC channel (length-prefixed JSON envelope). This document indexes the design spec; the implementation plan is `docs/superpowers/plans/2026-04-30-v0.3-daemon-split.md`.

## Canonical layout (Task #1043 dedup, 2026-05-01)

The spec was originally drafted as seven parallel fragments and then concatenated into this file. Both copies were maintained in parallel for several review rounds, which let drift creep in (frag-11 `perMachine` and frag-12 traceability rows were updated to r12 in PR #757 / #758 but the embedded copies here lagged behind).

To stop the drift permanently, **the fragment files are the single source of truth**:

- All section bodies (`§3.4.1`, `§3.5.1`, `§3.7`, `§6`, `§7`, `§8`, `§11`, `§12`) live in their owning `frag-*.md`.
- This file keeps only the intro, status, and the per-fragment index below.
- Any future spec edit lands in the fragment file and only the fragment file. Cross-references from code/tests use the existing `v0.3-design.md §X.Y` convention; readers follow the index here to the fragment file that owns `§X.Y`.

## Section index

| Spec section(s) | Topic | Canonical fragment |
| --- | --- | --- |
| §3.4.1 (a–h) | Envelope hardening — frame cap, chunk + binary format, hello/HMAC handshake, two-socket topology | [`v0.3-fragments/frag-3.4.1-envelope-hardening.md`](./v0.3-fragments/frag-3.4.1-envelope-hardening.md) |
| §3.5.1 | PTY child lifecycle hardening — supervisor, drop-slowest watermark, subscribe contract | [`v0.3-fragments/frag-3.5.1-pty-hardening.md`](./v0.3-fragments/frag-3.5.1-pty-hardening.md) |
| §3.7 | Dev workflow — daemon dev loop, reconnect bridge, SDK ownership, hot-reload | [`v0.3-fragments/frag-3.7-dev-workflow.md`](./v0.3-fragments/frag-3.7-dev-workflow.md) |
| §6 + §7 | Reliability + security expansion — supervisor, marker semantics, `daemon.shutdown*` RPCs, peer-cred, ACLs | [`v0.3-fragments/frag-6-7-reliability-security.md`](./v0.3-fragments/frag-6-7-reliability-security.md) |
| §8 | Data migration v0.2 → v0.3 (SQLite) | [`v0.3-fragments/frag-8-sqlite-migration.md`](./v0.3-fragments/frag-8-sqlite-migration.md) |
| §11 (incl. §11.2, §11.5, §11.6) | Packaging — Electron exact pin, NSIS install root, native ABI matrix, paths table | [`v0.3-fragments/frag-11-packaging.md`](./v0.3-fragments/frag-11-packaging.md) |
| §12 | Review traceability — round-by-round closure log + cross-fragment lock table | [`v0.3-fragments/frag-12-traceability.md`](./v0.3-fragments/frag-12-traceability.md) |

## How to read

- **Reviewer / new contributor**: start with the exec summary [`v0.3-daemon-split.md`](./v0.3-daemon-split.md), then `frag-3.4.1` (envelope) + `frag-6-7` (reliability/security) for the wire + lifecycle picture.
- **Implementer of a specific area**: jump straight to the fragment file from the table above. Each fragment carries its own `## Plan delta` and `## Cross-frag rationale` sections preserving the round-by-round audit trail (`[manager rN lock: ...]` annotations explain why each clause is shaped as it is).
- **Manager planning dispatch**: read the `## Plan delta` block at the bottom of each fragment plus the phase outline in `v0.3-daemon-split.md`.
- **Code spec-ref convention**: source files reference sections as `v0.3-design.md §X.Y` (existing convention, preserved). Resolve via the section index above to the owning fragment.

## Lifecycle note

Earlier the fragment README declared the fragments would be deleted after merge into this file. That plan is reversed as of Task #1043: fragments are retained as the canonical spec; this file is the index. The README in `v0.3-fragments/` documents the original drafting workflow (parallel review rounds) but no longer reflects the lifecycle.
