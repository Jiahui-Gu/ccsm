# R1 (feature-preservation) — round 2 review

**Scope**: re-verify v0.2 → v0.3 user-visible feature preservation after F4-F12 fixers landed (HEAD `90eede4`). Reference: round-1 R1 reviews on chapters 01 / 07 / 08 / 09 + dispatch plan F6.

**Verdict**: **NEEDS-FIX** — one P0 (manager-pending brief amendment is still the gating gap; until it lands, ship-gate has no v0.2-parity check and the §2 non-goals table has no "v0.2 features intentionally dropped" rows). All F6-owned per-chapter fixes are landed and feature-preserving; no new feature drop introduced. Once the manager amendment lands, this becomes PASS subject to a quick re-verify of 01-overview §1 + §2 + ship-gate (e).

---

## Summary of round-1 → round-2 closure

Round-1 R1 raised 6 P0 + 5 P1 (chapter 08), 2 P0 + 2 P1 (chapter 01), 0 P0 + 2 P1 (chapter 09), and downstream P1s on chapter 07. F6 (per dispatch plan §2) closed all per-chapter mechanical items. The remaining gap is the **brief amendment** (dispatch plan §3) which is **not a fixer** — it is manager-handled, and as of `90eede4` it has not been performed.

| Round-1 finding | Closure status | Where |
|---|---|---|
| 08-P0.1 IPC inventory missing ~25+ channels | **CLOSED** | 08 §2 table now enumerates A-I + crashReporting (matches R1 round-1 enumeration 1:1); §3 maps every row to a/b/c/d disposition with the four-bucket contract |
| 08-P0.2 Settings RPC drops ~10+ persisted prefs | **CLOSED** | 04 §6 adds `Settings.ui_prefs` map + `detected_claude_default_model` + `user_home_path` + `locale` + `sentry_enabled`; 08 §3 routes `db:load/save` → `Settings.ui_prefs`; drafts split out to `DraftService` |
| 08-P0.3 Custom titlebar window controls broken | **CLOSED** | 08 §3.1 `.no-ipc-allowlist` (FROZEN) carries `window:*` channels; lint scoped file-by-file under `ipc-allowlisted/` |
| 08-P0.4 Notify pipeline (toast/badge/flash/OSC-title) dropped | **CLOSED** | 04 §6.1 new `NotifyService` with `WatchNotifyEvents` stream + `MarkUserInput`/`SetActiveSid`/`SetFocused` setters; daemon owns the 7-rule decider |
| 08-P0.5 Session rename + import scan have no RPC | **CLOSED** | 04 §3 adds `RenameSession`, `GetSessionTitle`, `ListProjectSessions`, `ListImportableSessions`, `ImportSession` |
| 08-P0.6 `cwd:pick` no replacement | **CLOSED** | 08 §3 maps to `(c) electron-main-only`; allowlisted in §3.1 |
| 08-P1.1 v0.2 user-data migration | **PARTIALLY CLOSED** | F5 owns chapter 07 §4.5; F6 owns the migration narrative. Verify 07 has the §4.5 subsection (see F5 verification — out of R1 round-2 scope but flagged below). |
| 08-P1.3 daemon crash → blank screen UX | **CLOSED** | 08 §6.1 specifies 8s cold-start budget → blocking modal with troubleshooting link; renderer-only, no IPC |
| 08-P1.4 drafts loss-on-restart | **CLOSED** | 04 §6.2 `DraftService.{GetDraft, UpdateDraft}` with empty-string-deletes semantics |
| 08-P1.5 in-app updater UI deleted | **CLOSED** | 08 §3.1 allowlists `updates:*` as electron-main-only |
| 09-P1.1 two crash-reporting systems / Sentry toggle no home | **CLOSED** | 04 §6 `Settings.sentry_enabled` (default true); 09 §5 reads from it |
| 09-P1.2 daemon crashes after Electron exit invisible | **CLOSED** | 09 §6.1 "Crashes since you last looked" badge |
| 01-P0.1 Goals omits feature parity | **OPEN — manager-pending** | brief §1.8 not added; 01-overview §1 unchanged |
| 01-P0.2 Non-goals lists only v0.4 deferrals | **OPEN — manager-pending** | brief §2 second table not added; 01-overview §2 unchanged |
| 01-P1.1 No mention of user-data migration in §3 | **OPEN — partially manager-pending** | brief §3 unchanged |
| 01-P1.2 Glossary missing "v0.2 baseline" | **OPEN — manager-pending** | brief §6 unchanged |

---

## Per-chapter findings (round 2)

### 00-brief.md — **P0 (manager-pending)**

**P0.1 (carry-over from 01-P0.1 / 01-P0.2 / dispatch plan §3)** — Brief amendment not yet applied:

- Goal §1.8 ("Feature parity with v0.2") **MISSING**. `grep "1\.8\|feature parity"` in `00-brief.md` returns 0 hits. Without this goal, ship-gate has no v0.2-parity teeth and chapters 04 / 08 are not held to the parity bar even after F6 fixed them in spirit.
- Ship-gate §11(e) ("v0.2 feature-parity check" with v0.2-feature-checklist run by tester) **MISSING**. The four ship-gates (a)–(d) cover plumbing only; no gate exercises rename / SDK titles / import / notify / titlebar / cwd picker / drafts / theme persistence / updater / clipboard.
- §2 non-goals second table ("v0.2 features intentionally dropped in v0.3") **MISSING**. Currently the only explicitly-cut row in F6's chapter 08 §3 is `app:open-external` (replaced by `window.open` + `GetRawCrashLog`). That single drop should land in the §2 table once the table is added.
- §6 glossary entry "v0.2 baseline = the Electron-only single-process app shipped at tag v0.2.0 (commit X)" **MISSING**.
- §11(d) clarification (Windows-only ship-gate (d)) **MISSING**.
- §11(a) clarification (`.no-ipc-allowlist` mechanism replacing earlier "no allowlist" wording) **MISSING** — and this one is doubly important because F6's chapter 08 §3.1 introduces a non-empty allowlist, which contradicts the unamended brief §11(a) "0 hits" framing. Without this clarification, the brief and chapter 08 are inconsistent at the spec layer (R5 issue technically, but it lives inside R1's parity story because the allowlist is what preserves window controls, cwd picker, and updater).

**Suggested fix**: manager applies the dispatch-plan §3 brief amendment as written. Estimated ~30 lines of edits across `00-brief.md` §1, §2, §6, §11.

### 01-overview.md — **P0 (cascades from brief)**

**P0.1 (carry-over from round-1)** — `grep "parity\|baseline\|v0\.2"` in `01-overview.md` returns 0 hits. §1 still lists 7 goals without feature parity; §2 still lists v0.4 deferrals only; §3 scope statement still doesn't address user-data migration; §6 glossary still lacks v0.2 baseline.

**Note**: this chapter chain-edits FROM the brief amendment — once the brief lands, 01-overview §1 / §2 / §3 / §6 must be refreshed in lockstep (single PR, manager-handled). Reviewer will confirm closure on the next R1 pass.

### 04-proto-and-rpc-surface.md — **PASS**

Verified F6 RPC additions present and well-shaped:

- `SessionService`: `RenameSession` (line 117), `GetSessionTitle` (118), `ListProjectSessions` (119), `ListImportableSessions` (120), `ImportSession` (121) — all 5 present with full message types.
- `PtyService.CheckClaudeAvailable` (line 330) with response carrying `available` / `resolved_path` / `version` / `error_code`.
- `CrashService.GetRawCrashLog` (line 443) with chunked stream + `eof` bit; correctly noted as not owner-scoped (daemon-self by definition); peer-cred middleware still scopes admin in v0.4.
- `Settings` extensions: `ui_prefs` (591), `detected_claude_default_model` (597), `user_home_path` (605), `locale` (611), `sentry_enabled` (617). `claude_binary_path` correctly EXCLUDED from RPC and pinned to install-time config (closes R2 P0-04-3 escalated).
- `NotifyService` (§6.1): `WatchNotifyEvents` + `MarkUserInput` + `SetActiveSid` + `SetFocused`; payload covers TOAST / BADGE / FLASH / TITLE; per-principal scope is implicit via peer-cred (correct for v0.3 single-user).
- `DraftService` (§6.2): `GetDraft` / `UpdateDraft`; empty-string-deletes matches v0.2 semantics.
- `Session.runtime_pid` (line 234) added for ship-gate (b) verification harness step 6.

No additional RPC gaps observed against the round-1 R1 enumeration. Open-string-set semantics (`client_kind`, `CrashEntry.source`, `flash_pattern`, `listener_id`) are consistently documented.

### 08-electron-client-migration.md — **PASS**

- §2 inventory now mirrors round-1 R1's enumeration 1:1 (sources A–I from R1 round-1 + crashReporting); F6 commit comment correctly cites "produced by `grep -rn ipcMain\\.handle|ipcMain\\.on|contextBridge\\.exposeInMainWorld electron/`".
- §3 disposition table covers every §2 row with one of (a) Connect RPC, (b) renderer-only, (c) electron-main-only allowlisted, (d) explicitly-cut. No silent drops.
- The only `(d) explicitly-cut` row is `app:open-external` → replaced by `window.open` for `https?://` + `GetRawCrashLog` for daemon-side files. Reasonable; symmetric across Electron/web/iOS; UI delta is "Open raw log file" → "Download raw log".
- §3.1 `.no-ipc-allowlist` is FROZEN with finite enumeration: `window:*` (7 entries) + `cwd:pick` (1) + `updates:*` (7). All three clusters have the right rationale (no daemon home, no browser equivalent, no UX regression alternative).
- §3.2 `safe-open-url.ts` covers URL safety with explicit scheme allowlist.
- §6.1 cold-start UX (8s budget, blocking modal, retry-and-troubleshoot affordance) is concrete and feature-restoring.
- §6.2 React Query layer documented as forever-stable abstraction.
- §7 verification harness step 6 uses the new `Session.runtime_pid` per-OS probe.

**Minor observation (not blocking)**: §3 line 86 says "ship-gate (a) admits a small allowlist for OS-chrome concerns to preserve frameless UX" — this language is correct as a chapter-08 statement, but until brief §11(a) is amended (see brief P0.1 above), there is a wording-level inconsistency between brief and chapter. This is captured under brief P0.1 and not flagged separately.

### 09-crash-collector.md — **PASS**

- §5 Sentry toggle reads `Settings.sentry_enabled` (line 76); default true matching v0.2; SQLite log independent and always-on.
- §6.1 "Crashes since you last looked" badge spec converts silent-crash failure into in-app passive signal. Renderer stores `last_seen_crash_id` in `Settings.ui_prefs["crash.last_seen_id"]`.
- §6.2 capture-sources table-driven via `packages/daemon/src/crash/sources.ts` with auto-iterating tests (also closes R3 P1-09-3 capture-source unification).

### 07-data-and-state.md — **OUT OF R1 ROUND-2 SCOPE (verify under F5)**

Round-1 R1 P1.1 (v0.2 → v0.3 user-data migration) is owned by F5 (chapter 07 §4.5 subsection). F6 owns the migration narrative that lives in 08, but the schema-side migration plan lives in 07. Not in R1's round-2 verification scope; flagging here so the round-2 R0/R5 review can confirm the §4.5 subsection landed.

---

## Internal-completeness check on chapter 08 §3 dispositions

Cross-check ("are the explicitly-cut rows reasonable?"):

- Only `app:open-external` is explicitly-cut. Rationale (no `file://`, no daemon-side path opening; replaced by safe `window.open` + `GetRawCrashLog` "Download raw log" UI) is sound and symmetric across all three future client kinds.
- No other v0.2 channel is silently dropped — every row in §2 has a non-`(d)` disposition with concrete replacement RPC or electron-main-only allowlist entry.

No P0 from this angle.

---

## Final verdict

**NEEDS-FIX** — single open P0: brief amendment per dispatch plan §3 not yet applied. F4–F12 per-chapter fixers themselves all preserve features correctly; the remaining gap is upstream of any fixer (manager-handled). Once the brief amendment + cascading 01-overview refresh land, this becomes PASS.

**Re-verify scope on next R1 pass**:
1. `00-brief.md` §1.8 + §2 second table + §6 v0.2 baseline + §11(d) + §11(e) + §11(a) clarification.
2. `01-overview.md` §1 + §2 + §3 + §6 mirror the brief.
3. `07-data-and-state.md` §4.5 v0.2 → v0.3 user-data migration subsection (cross-check from F5).
